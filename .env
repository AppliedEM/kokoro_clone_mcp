# Host user ID for audio device access (Option B - auto_play)
# This is used to mount the PulseAudio/PipeWire socket into the container
HOST_UID=1000

# Uncomment and modify if your host uses a different UID:
# HOST_UID=501  # macOS default
