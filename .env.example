# Kokoro TTS MCP Server Configuration
# Copy this file to .env and modify as needed

# Default voice (af_bella, am_michael, etc.)
KOKORO_VOICE=af_bella

# Compute device: cpu, cuda, auto
KOKORO_DEVICE=cpu

# Model quality: fp32 (best), fp16 (balanced), q8 (fast), q4 (fastest)
KOKORO_QUALITY=fp16
